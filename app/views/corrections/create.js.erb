$('.correction-errors').html('<%= render 'shared/errors', resource: @correction %>'); 

<% unless @correction.errors.present? %>
  $('#corrections-answer-' + <%= @answer.id %>).append("<%= j render @correction %>")
  $('.new-correction-form').hide();
  $('.correction-link').toggle();
<% end %>